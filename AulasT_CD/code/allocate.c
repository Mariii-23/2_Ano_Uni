int main(){
    int* ptr;

    ptr = ()malloc( number * sizeof() );

    if (ptr == NULL){
        printf("Memory not allocated.\n ");
        exit(0);
    } else {
        for (i=0; i< .. ; i++){
            printf("%d, ", ptr[i]);
        }
    }

    return 0;
}
